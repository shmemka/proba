# 🔧 Устранение проблем с DNS для proba.space

## Проблема
Домен `https://www.proba.space` не открывается с десктопа, но работает с телефона.

## ✅ Диагностика показала
- DNS резолвится правильно: `www.proba.space` → `e6ac534bb8a306dd.vercel-dns-017.com`
- IP адреса: `64.29.17.1`, `216.198.79.1`
- Сайт доступен через curl (HTTP 200)
- SSL сертификат валидный (Let's Encrypt)
- **Вывод:** Проблема не в DNS или сервере, а скорее всего в кеше браузера/системы

## ✅ Проверка DNS записей

DNS записи настроены правильно:
- `www.proba.space` → `e6ac534bb8a306dd.vercel-dns-017.com` (Vercel DNS)
- IP адреса: `64.29.17.1`, `216.198.79.1`
- Сайт отвечает HTTP 200

## 🔧 Решения

### 1. Очистка DNS кеша на вашем компьютере

#### Для macOS:
```bash
sudo dscacheutil -flushcache
sudo killall -HUP mDNSResponder
```

#### Для Windows:
Откройте командную строку (cmd) от имени администратора:
```cmd
ipconfig /flushdns
```

#### Для Linux:
```bash
sudo systemd-resolve --flush-caches
# или
sudo service network-manager restart
```

### 2. Очистка кеша браузера

1. **Chrome/Edge:**
   - Нажмите `Cmd+Shift+Delete` (Mac) или `Ctrl+Shift+Delete` (Windows)
   - Выберите "За все время"
   - Отметьте "Изображения и другие файлы, сохраненные в кеше"
   - Нажмите "Удалить данные"

2. **Firefox:**
   - Нажмите `Cmd+Shift+Delete` (Mac) или `Ctrl+Shift+Delete` (Windows)
   - Выберите "Кеш"
   - Нажмите "Очистить сейчас"

3. **Safari:**
   - Safari → Настройки → Дополнения
   - Включите "Показывать меню "Разработка""
   - Разработка → Очистить кеши

### 3. Проверка настроек DNS в Рег.ру

1. Войдите в панель управления Рег.ру
2. Перейдите к управлению доменом `proba.space`
3. Откройте раздел **DNS/DNS-серверы**

4. **Проверьте записи:**
   - Должна быть A-запись или CNAME для `www`:
     ```
     Тип: CNAME
     Имя: www
     Значение: e6ac534bb8a306dd.vercel-dns-017.com
     ```
   
   - Или A-запись для `www`:
     ```
     Тип: A
     Имя: www
     Значение: IP адрес Vercel
     ```

5. **Проверьте DNS-серверы:**
   - Должны быть указаны DNS-серверы Vercel:
     ```
     ns1.vercel-dns.com
     ns2.vercel-dns.com
     ```
   - Или DNS-серверы Рег.ру (тогда записи должны быть настроены в Рег.ру)

### 4. Проверка настроек в Vercel

1. Откройте [Vercel Dashboard](https://vercel.com)
2. Перейдите в ваш проект
3. Откройте **Settings** → **Domains**
4. Убедитесь, что домен `www.proba.space` добавлен
5. Проверьте статус домена - должен быть "Valid Configuration"

### 5. Использование другого DNS-сервера

Попробуйте использовать публичные DNS-серверы:

#### Изменить DNS на компьютере:

**macOS:**
1. Системные настройки → Сеть
2. Выберите ваше подключение → Дополнительно → DNS
3. Добавьте DNS-серверы:
   - `8.8.8.8` (Google)
   - `1.1.1.1` (Cloudflare)

**Windows:**
1. Панель управления → Сеть и Интернет → Центр управления сетями
2. Изменить параметры адаптера
3. Правой кнопкой на ваше подключение → Свойства
4. Протокол Интернета версии 4 (TCP/IPv4) → Свойства
5. Использовать следующие адреса DNS-серверов:
   - `8.8.8.8`
   - `1.1.1.1`

### 6. Проверка с разных браузеров

Попробуйте открыть сайт в:
- Chrome
- Firefox
- Safari
- Edge

Если в одном браузере работает, а в другом нет - проблема в кеше браузера.

### 7. Проверка через приватное окно

Откройте сайт в приватном/инкогнито режиме:
- Chrome/Edge: `Cmd+Shift+N` (Mac) или `Ctrl+Shift+N` (Windows)
- Firefox: `Cmd+Shift+P` (Mac) или `Ctrl+Shift+P` (Windows)
- Safari: `Cmd+Shift+N`

### 8. Проверка файла hosts

Убедитесь, что домен не заблокирован в файле hosts:

**macOS/Linux:**
```bash
sudo nano /etc/hosts
```

**Windows:**
```
C:\Windows\System32\drivers\etc\hosts
```

Убедитесь, что там нет строки с `proba.space`.

### 9. Проверка антивируса/файрвола

Проверьте, не блокирует ли антивирус или файрвол доступ к сайту:
- Временно отключите антивирус/файрвол
- Попробуйте открыть сайт
- Если заработало - добавьте исключение

### 10. Проверка с мобильного интернета на компьютере

1. Создайте точку доступа на телефоне
2. Подключите компьютер к мобильному интернету
3. Попробуйте открыть сайт

Если заработало - проблема в вашем домашнем интернет-провайдере или роутере.

### 11. Проверка HSTS (HTTP Strict Transport Security)

**Это самая частая причина проблемы "работает на телефоне, но не на десктопе"!**

HSTS может блокировать подключение, если браузер запомнил неправильную информацию о домене.

**Chrome/Edge:**
1. Откройте `chrome://net-internals/#hsts` (или `edge://net-internals/#hsts`)
2. Найдите раздел "Query HSTS/PKP domain"
3. Введите `proba.space` и проверьте, что нет старых записей
4. Если есть - удалите через "Delete domain security policies"

**Firefox:**
1. Откройте `about:config`
2. Поищите `security.tls.insecure_fallback_hosts`
3. Убедитесь, что `proba.space` не заблокирован

### 12. Проверка файла hosts (может блокировать домен)

**macOS/Linux:**
```bash
sudo cat /etc/hosts | grep proba
```

Если есть запись с `proba.space` - удалите её:
```bash
sudo nano /etc/hosts
# Удалите строку с proba.space
```

**Windows:**
Откройте `C:\Windows\System32\drivers\etc\hosts` в блокноте от имени администратора
Убедитесь, что там нет записей с `proba.space`

### 13. Временное использование другого DNS

Если проблема в DNS провайдера, временно используйте публичные DNS:

**macOS:**
1. Системные настройки → Сеть
2. Выберите ваше подключение → Дополнительно → DNS
3. Добавьте: `8.8.8.8` и `1.1.1.1`
4. Очистите DNS кеш (см. выше)

**Windows:**
1. Панель управления → Сеть → Свойства адаптера
2. TCP/IPv4 → Свойства
3. Использовать следующие DNS: `8.8.8.8`, `1.1.1.1`
4. Выполните `ipconfig /flushdns`

## 🔍 Диагностика

### Проверка DNS на вашем компьютере:

**macOS/Linux:**
```bash
dig www.proba.space
nslookup www.proba.space
```

**Windows:**
```cmd
nslookup www.proba.space
```

### Проверка доступности сайта:

```bash
curl -I https://www.proba.space
ping www.proba.space
```

### Проверка SSL сертификата:

```bash
openssl s_client -connect www.proba.space:443 -servername www.proba.space
```

## ⚡ Быстрое решение (для macOS)

Выполните **все** команды по порядку:

1. **Очистите DNS кеш системы:**
   ```bash
   sudo dscacheutil -flushcache
   sudo killall -HUP mDNSResponder
   ```

2. **Очистите HSTS кеш (критично!):**
   - **Chrome/Edge:**
     - Откройте `chrome://net-internals/#hsts` (или `edge://net-internals/#hsts`)
     - Прокрутите вниз до "Delete domain security policies"
     - Введите `proba.space` и нажмите "Delete"
     - Закройте браузер полностью
   
   - **Firefox:**
     - Откройте `about:config`
     - Найдите `network.stricttransportsecurity.preloadlist` и `network.stricttransportsecurity.preloaded`
     - Очистите эти значения
     - Закройте браузер полностью

   - **Safari:**
     - Откройте терминал и выполните:
     ```bash
     rm ~/Library/Cookies/HSTS.plist
     killall cfprefsd
     ```

3. **Очистите кеш браузера:**
   - **Chrome/Edge:** `Cmd+Shift+Delete` → Выберите "За все время" → Отметьте "Кешированные изображения и файлы" → Удалить
   - **Firefox:** `Cmd+Shift+Delete` → Кеш → Очистить сейчас
   - **Safari:** Разработка → Очистить кеши

4. **Очистите cookies для домена:**
   - **Chrome/Edge:** `Cmd+Shift+Delete` → Cookies → Удалите cookies для `proba.space`
   - **Firefox:** Настройки → Приватность → Управление данными → Найдите `proba.space` → Удалите

5. **Закройте все окна браузера и перезапустите**

6. **Попробуйте открыть в приватном окне:**
   - Chrome/Edge: `Cmd+Shift+N`
   - Firefox: `Cmd+Shift+P`
   - Safari: `Cmd+Shift+N`

7. **Если не помогло, перезагрузите Mac:**
   ```bash
   sudo reboot
   ```

## ⚡ Быстрое решение (для Windows)

1. **Очистите DNS кеш:**
   ```cmd
   ipconfig /flushdns
   ```

2. **Очистите HSTS кеш:**
   - Откройте `chrome://net-internals/#hsts`
   - Удалите `proba.space` из HSTS списка
   - Закройте браузер

3. **Очистите кеш браузера** (см. выше)

4. **Перезагрузите компьютер**

## 🎯 Рекомендации для Рег.ру

Если используете DNS-серверы Рег.ру, убедитесь, что настроены следующие записи:

1. **CNAME для www:**
   ```
   Имя: www
   Тип: CNAME
   Значение: e6ac534bb8a306dd.vercel-dns-017.com
   TTL: 3600
   ```

2. **A-запись для корня (опционально):**
   ```
   Имя: @
   Тип: A
   Значение: 76.76.21.21 (IP Vercel)
   TTL: 3600
   ```

3. **Или используйте DNS-серверы Vercel:**
   ```
   ns1.vercel-dns.com
   ns2.vercel-dns.com
   ```

## 📞 Если ничего не помогло

1. Проверьте логи в Vercel Dashboard → Deployments
2. Свяжитесь с поддержкой Рег.ру
3. Свяжитесь с поддержкой Vercel
4. Проверьте, нет ли проблем с вашим интернет-провайдером

## ✅ Проверка после исправления

После выполнения всех шагов:
1. Откройте https://www.proba.space в браузере
2. Проверьте, что сайт загружается
3. Проверьте, что все функции работают (логин, регистрация и т.д.)

