# 🔧 Исправление ошибки 404 при входе через Google

## Проблема
При нажатии на кнопку "Войти через Google" возникает ошибка **"this page could not be found 404"**.

## Возможные причины и решения

### 1. ✅ Проверьте Redirect URLs в Supabase Dashboard

**Самая частая причина!**

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Выберите ваш проект
3. Перейдите в **Authentication** → **URL Configuration**
4. Проверьте раздел **Redirect URLs**
5. Убедитесь, что добавлены следующие URL:

   ```
   https://www.proba.space/auth/callback
   http://localhost:3000/auth/callback (для локальной разработки)
   ```

6. Если их нет - добавьте, нажмите **Add URL**, введите URL, нажмите **Save**

### 2. ✅ Проверьте Site URL в Supabase

1. В том же разделе **Authentication** → **URL Configuration**
2. Проверьте **Site URL**:
   - Должен быть: `https://www.proba.space`
   - (Для локальной разработки: `http://localhost:3000`)

### 3. ✅ Проверьте переменную окружения в Vercel

1. Откройте [Vercel Dashboard](https://vercel.com)
2. Перейдите в **Settings** → **Environment Variables**
3. Проверьте переменную `NEXT_PUBLIC_SITE_URL`:
   - Должна быть: `https://www.proba.space`
4. Если её нет - добавьте
5. **Важно:** После изменения переменной **перезапустите деплой**!

### 4. ✅ Проверьте URL в консоли браузера

1. Откройте сайт https://www.proba.space
2. Откройте DevTools (F12) → вкладка **Console**
3. Нажмите "Войти через Google"
4. В консоли должно появиться: `OAuth redirect URL: https://www.proba.space/auth/callback`
5. Если URL другой или неправильный - проблема в формировании URL

### 5. ✅ Проверьте, что страница callback существует

Убедитесь, что файл существует:
- `app/auth/callback/page.tsx`

Если его нет - создайте или восстановите из git.

### 6. ✅ Проверьте Google Cloud Console Redirect URI

1. Откройте [Google Cloud Console](https://console.cloud.google.com)
2. Перейдите в **APIs & Services** → **Credentials**
3. Найдите ваш OAuth 2.0 Client ID
4. Проверьте **Authorized redirect URIs**:
   - Должен быть: `https://[YOUR-PROJECT-REF].supabase.co/auth/v1/callback`
   - Замените `[YOUR-PROJECT-REF]` на ваш Project Reference из Supabase

## Диагностика

### Шаг 1: Проверьте консоль браузера

Откройте DevTools (F12) → Console и найдите:
- Какой URL используется для редиректа?
- Есть ли ошибки JavaScript?
- Есть ли ошибки сети в Network tab?

### Шаг 2: Проверьте Network tab

1. Откройте DevTools → Network
2. Нажмите "Войти через Google"
3. Посмотрите, какие запросы выполняются:
   - Запрос к Google должен быть успешным
   - Редирект обратно должен быть на правильный URL

### Шаг 3: Проверьте URL в адресной строке

Когда происходит 404, посмотрите на URL в адресной строке:
- Какой URL показывается?
- Соответствует ли он тому, что должно быть?

## Быстрое решение

Если проблема не решается, выполните все шаги по порядку:

1. ✅ Добавьте redirect URL в Supabase:
   ```
   https://www.proba.space/auth/callback
   ```

2. ✅ Установите Site URL в Supabase:
   ```
   https://www.proba.space
   ```

3. ✅ Добавьте переменную окружения в Vercel:
   ```
   NEXT_PUBLIC_SITE_URL=https://www.proba.space
   ```

4. ✅ Перезапустите деплой в Vercel

5. ✅ Очистите кеш браузера и попробуйте снова

## Если ничего не помогло

1. Проверьте логи в Supabase Dashboard → Logs → Auth Logs
2. Проверьте логи в Vercel Dashboard → Deployments → Functions
3. Свяжитесь с поддержкой Supabase, если проблема сохраняется

## Обновление кода

Я добавил улучшенную обработку URL и логирование в функцию `signInWithGoogle`. После деплоя обновленного кода в консоли браузера будет видно, какой URL используется для редиректа.

