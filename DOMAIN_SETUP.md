# 🌐 Настройка домена proba.space

Инструкция по настройке всех сервисов для работы с доменом `https://www.proba.space`.

## 📋 Что нужно настроить

### 1. ✅ Переменные окружения (Vercel)

1. Откройте ваш проект на [Vercel](https://vercel.com)
2. Перейдите в **Settings** → **Environment Variables**
3. Добавьте или обновите переменную:

   **Key:** `NEXT_PUBLIC_SITE_URL`  
   **Value:** `https://www.proba.space`  
   **Environment:** Production, Preview, Development

4. Нажмите **Save**
5. Перезапустите деплой (Vercel сделает это автоматически)

### 2. ✅ Supabase Dashboard - Site URL и Redirect URLs

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard)
2. Выберите ваш проект
3. Перейдите в **Authentication** → **URL Configuration**

4. **Site URL:**
   - Установите: `https://www.proba.space`

5. **Redirect URLs:**
   - Добавьте следующие URL (если их еще нет):
     ```
     https://www.proba.space/auth/callback
     https://www.proba.space/auth/confirm
     https://www.proba.space/auth/verified
     http://localhost:3000/auth/callback (для локальной разработки)
     http://localhost:3000/auth/confirm (для локальной разработки)
     ```

6. Нажмите **Save**

### 3. ✅ Google Cloud Console - OAuth настройки

1. Откройте [Google Cloud Console](https://console.cloud.google.com)
2. Выберите ваш проект
3. Перейдите в **APIs & Services** → **Credentials**
4. Найдите ваш OAuth 2.0 Client ID (например, "Proba")
5. Нажмите на название клиента для редактирования

6. **Authorized JavaScript origins:**
   - Добавьте (если еще нет):
     ```
     https://www.proba.space
     http://localhost:3000 (для локальной разработки)
     ```

7. **Authorized redirect URIs:**
   - Убедитесь, что добавлен:
     ```
     https://[YOUR-PROJECT-REF].supabase.co/auth/v1/callback
     ```
   - Замените `[YOUR-PROJECT-REF]` на ваш Project Reference из Supabase
   - Найти Project Reference: Supabase Dashboard → Settings → API → Project URL

8. Нажмите **Save**

### 4. ✅ Локальная разработка (.env.local)

Создайте или обновите файл `.env.local` в корне проекта:

```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
NEXT_PUBLIC_SUPABASE_STORAGE_BUCKET=public-assets
NEXT_PUBLIC_SITE_URL=http://localhost:3000
```

> ⚠️ **Важно:** Для локальной разработки используйте `http://localhost:3000`, для продакшена - `https://www.proba.space`

## ✅ Проверка работы

### После настройки проверьте:

1. **Регистрация через email:**
   - Зарегистрируйтесь на https://www.proba.space/register
   - Проверьте, что письмо с подтверждением приходит с правильными ссылками

2. **Вход через Google:**
   - Нажмите "Войти через Google" на https://www.proba.space/login
   - После авторизации вы должны вернуться на сайт

3. **Email подтверждение:**
   - Перейдите по ссылке из письма
   - Должно произойти перенаправление на https://www.proba.space/auth/confirm

## 🔧 Возможные проблемы

### Ошибка "redirect_uri_mismatch" при входе через Google
- **Решение:** Проверьте, что в Google Cloud Console добавлен правильный Redirect URI:
  ```
  https://[YOUR-PROJECT-REF].supabase.co/auth/v1/callback
  ```

### Ссылки в письмах ведут на localhost
- **Решение:** Проверьте, что в Supabase Dashboard → Authentication → URL Configuration установлен правильный Site URL: `https://www.proba.space`

### OAuth не работает после деплоя
- **Решение:** 
  1. Проверьте переменную окружения `NEXT_PUBLIC_SITE_URL` в Vercel
  2. Убедитесь, что перезапустили деплой после изменения переменных
  3. Проверьте, что домен правильно настроен в Vercel (Settings → Domains)

## 📝 Примечания

- Для локальной разработки используйте `http://localhost:3000`
- Для продакшена используйте `https://www.proba.space`
- После изменения настроек в Supabase или Google Cloud может потребоваться несколько минут для применения изменений
- Убедитесь, что домен `www.proba.space` правильно настроен в Vercel и указывает на ваш проект

## 🎉 Готово!

После выполнения всех шагов ваш сайт должен полностью работать с доменом `https://www.proba.space`.

