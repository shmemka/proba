# 🔧 Исправление ошибки "company_id does not exist"

## Проблема

```
ERROR: 42703: column "company_id" does not exist
```

Это происходит потому что у вас уже есть таблицы в Supabase старой структуры (без колонки `company_id`).

---

## ✅ Решение (выберите один вариант)

### Вариант 1: Обновить существующую БД (РЕКОМЕНДУЕТСЯ)

**Используйте если:** у вас есть важные данные, которые нужно сохранить

**Шаги:**
1. В Supabase откройте **SQL Editor**
2. Нажмите "New query"
3. Откройте файл **`supabase-migration-UPDATE.sql`**
4. Скопируйте **весь код**
5. Вставьте в SQL Editor
6. Нажмите **Run** (Ctrl/Cmd + Enter)
7. Дождитесь сообщения "Migration completed!"

**Что делает этот скрипт:**
- ✅ Добавляет недостающую колонку `company_id`
- ✅ Автоматически связывает старые проекты с первой компанией
- ✅ Обновляет все constraints и политики
- ✅ **Сохраняет все ваши данные**

---

### Вариант 2: Создать БД заново (если данные не важны)

**Используйте если:** можно удалить все данные и начать заново

**Шаги:**
1. В Supabase откройте **SQL Editor**
2. Нажмите "New query"
3. Выполните этот код для удаления таблиц:

```sql
-- Удаляем таблицы в правильном порядке (из-за foreign keys)
DROP TABLE IF EXISTS applications CASCADE;
DROP TABLE IF EXISTS projects CASCADE;
DROP TABLE IF EXISTS specialists CASCADE;
DROP TABLE IF EXISTS companies CASCADE;
```

4. Теперь откройте файл **`supabase-migration.sql`**
5. Скопируйте и выполните весь код
6. Готово!

---

## 🔍 Проверка после исправления

### 1. Проверьте структуру таблиц

В SQL Editor выполните:
```sql
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'projects' 
ORDER BY ordinal_position;
```

Должны быть колонки:
- ✅ `id`
- ✅ `company_id` ← эта колонка должна быть!
- ✅ `title`
- ✅ `description`
- ✅ и другие...

### 2. Проверьте данные

```sql
SELECT * FROM projects LIMIT 5;
SELECT * FROM companies LIMIT 5;
```

### 3. Проверьте в приложении

```bash
npm run dev
```

Откройте консоль браузера (F12):
- ✅ Должно быть: `"✅ Supabase клиент инициализирован"`
- ✅ Попробуйте создать проект

---

## 📝 Что дальше после исправления?

### Если у вас были старые проекты

Старые проекты автоматически привязаны к первой компании в БД.

**Чтобы исправить владельца проекта:**

1. Найдите ID нужной компании:
```sql
SELECT id, name FROM companies;
```

2. Обновите проекты:
```sql
UPDATE projects 
SET company_id = 'вставьте_id_компании_здесь' 
WHERE id = 'вставьте_id_проекта_здесь';
```

### Если нужно создать тестовые данные

```sql
-- Создать тестовую компанию (после регистрации пользователя)
INSERT INTO companies (id, name, email)
VALUES (
  'вставьте_user_id_из_auth_users', 
  'Тестовая компания', 
  'test@example.com'
);
```

---

## ❌ Если ничего не помогло

### Вариант 3: Полная очистка и пересоздание

```sql
-- 1. Удаляем всё
DROP SCHEMA public CASCADE;
CREATE SCHEMA public;
GRANT ALL ON SCHEMA public TO postgres;
GRANT ALL ON SCHEMA public TO public;

-- 2. Включаем расширения
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- 3. Теперь выполните supabase-migration.sql
```

---

## 🆘 Дополнительная помощь

### Проверьте Auth пользователей

```sql
-- Посмотрите зарегистрированных пользователей
SELECT id, email, 
  raw_user_meta_data->>'userType' as user_type,
  raw_user_meta_data->>'displayName' as display_name
FROM auth.users;
```

### Проверьте RLS политики

```sql
-- Посмотрите все политики
SELECT schemaname, tablename, policyname 
FROM pg_policies 
WHERE schemaname = 'public';
```

---

## ✅ Контрольный список

После исправления проверьте:

- [ ] Выполнен `supabase-migration-UPDATE.sql`
- [ ] В таблице `projects` есть колонка `company_id`
- [ ] В консоли браузера: "✅ Supabase клиент инициализирован"
- [ ] Можно зарегистрировать специалиста
- [ ] Можно зарегистрировать компанию
- [ ] Компания может создать проект
- [ ] Специалист может подать заявку

---

## 📚 Файлы для миграции

Используйте правильный файл:

| Файл | Когда использовать |
|------|-------------------|
| `supabase-migration.sql` | Новая БД с нуля |
| `supabase-migration-UPDATE.sql` | **Обновление существующей БД** ← ИСПОЛЬЗУЙТЕ ЭТО |

---

Готово! Используйте **`supabase-migration-UPDATE.sql`** и ошибка исчезнет! 🎉



