# ⚡ Исправление проблемы с зависанием сайта

## Проблема
Сайт зависает или очень долго грузится на кастомном домене, хотя на домене Vercel работает нормально.

## ✅ Исправления

### 1. Добавлены таймауты для всех запросов к Supabase
- Все запросы теперь имеют таймаут 10 секунд
- При превышении таймаута запрос прерывается, сайт не зависает

### 2. Улучшена обработка ошибок
- При ошибках сети или таймаутах сайт продолжает работать
- Ошибки логируются, но не блокируют загрузку страницы

### 3. Исправлены метаданные
- Обновлен URL в Open Graph метаданных на `https://proba.space`

## 🔧 Что было изменено

### `lib/cache.ts`
- Добавлена функция `withTimeout` для всех запросов
- Таймаут: 10 секунд

### `lib/supabaseClient.ts`
- Добавлен таймаут для fetch запросов (только в браузере)
- Запросы автоматически прерываются через 10 секунд

### `hooks/useAuthUser.ts`
- Улучшена обработка ошибок при загрузке пользователя
- При ошибках сайт продолжает работать без блокировки

## 📋 Что нужно сделать

1. **Сделайте redeploy в Vercel:**
   - Vercel Dashboard → Deployments → последний деплой → Redeploy

2. **Проверьте переменные окружения:**
   - Убедитесь, что `NEXT_PUBLIC_SITE_URL` установлена в `https://proba.space`
   - Vercel Dashboard → Settings → Environment Variables

3. **Очистите кеш браузера:**
   - Откройте сайт в приватном окне
   - Или очистите кеш: Cmd+Shift+Delete (Mac) / Ctrl+Shift+Delete (Windows)

## 🧪 Проверка

После redeploy проверьте:
1. Сайт загружается без зависаний
2. В консоли браузера (F12) нет критических ошибок
3. Если есть ошибки сети, они логируются, но не блокируют сайт

## ⚠️ Если проблема остается

1. **Проверьте консоль браузера (F12):**
   - Откройте вкладку Console
   - Посмотрите, какие ошибки появляются

2. **Проверьте вкладку Network:**
   - Какие запросы зависают?
   - Есть ли запросы с таймаутом?

3. **Проверьте переменные окружения в Vercel:**
   - `NEXT_PUBLIC_SUPABASE_URL` - должен быть установлен
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY` - должен быть установлен
   - `NEXT_PUBLIC_SITE_URL` - должен быть `https://proba.space`

4. **Проверьте DNS:**
   - Убедитесь, что DNS правильно настроен (см. `DOMAIN_SETUP_REG.RU.md`)

