# 🔧 Настройка домена Рег.ру для Vercel

## ❌ Проблема
Сайт работает на домене Vercel (например, `your-project.vercel.app`), но не работает на купленном домене от Рег.ру.

## ✅ Решение: Правильная настройка DNS

### Шаг 1: Добавьте домен в Vercel

1. Откройте [Vercel Dashboard](https://vercel.com)
2. Выберите ваш проект
3. Перейдите в **Settings** → **Domains**
4. Нажмите **"Add Domain"**
5. Введите ваш домен (например: `example.ru`)
6. Введите также `www.example.ru`
7. Нажмите **"Add"**

Vercel покажет вам инструкции по настройке DNS.

### Шаг 2: Настройка DNS в Рег.ру

Есть **два способа** настройки:

---

## 🔹 Способ 1: Использование DNS-серверов Vercel (РЕКОМЕНДУЕТСЯ)

Это самый простой и надежный способ.

### 1. Получите DNS-серверы Vercel

В панели Vercel (Settings → Domains) вы увидите DNS-серверы, например:
```
ns1.vercel-dns.com
ns2.vercel-dns.com
```

### 2. Настройте в Рег.ру

1. Войдите в [панель управления Рег.ру](https://www.reg.ru/user)
2. Перейдите в **Домены** → выберите ваш домен
3. Откройте раздел **DNS-серверы** (или **NS-записи**)
4. Выберите **"Использовать DNS-серверы"**
5. Введите DNS-серверы Vercel:
   ```
   ns1.vercel-dns.com
   ns2.vercel-dns.com
   ```
6. Нажмите **"Сохранить"**

### 3. Подождите распространения DNS

- Обычно занимает **5-30 минут**
- Может занять до **24 часов** (редко)

### 4. Проверьте в Vercel

Вернитесь в Vercel Dashboard → Settings → Domains. Статус должен измениться на **"Valid Configuration"** (зеленая галочка).

---

## 🔹 Способ 2: Использование DNS-записей в Рег.ру

Если вы хотите использовать DNS-серверы Рег.ру, настройте записи вручную.

### 1. Получите значения из Vercel

В Vercel Dashboard → Settings → Domains вы увидите, какие записи нужно добавить.

Обычно это:

**Для корневого домена (example.ru):**
- Тип: **A**
- Имя: **@** (или оставить пустым)
- Значение: IP адрес Vercel (например: `76.76.21.21`)
- TTL: `3600`

**Для www (www.example.ru):**
- Тип: **CNAME**
- Имя: **www**
- Значение: `cname.vercel-dns.com` (или значение, которое показал Vercel)
- TTL: `3600`

### 2. Настройте в Рег.ру

1. Войдите в панель управления Рег.ру
2. Перейдите в **Домены** → выберите ваш домен
3. Откройте раздел **DNS-записи** (или **Управление DNS**)
4. Убедитесь, что выбраны **DNS-серверы Рег.ру**
5. Добавьте записи:

   **Запись 1 (A для корня):**
   ```
   Тип: A
   Имя: @
   Значение: 76.76.21.21
   TTL: 3600
   ```

   **Запись 2 (CNAME для www):**
   ```
   Тип: CNAME
   Имя: www
   Значение: cname.vercel-dns.com
   TTL: 3600
   ```

   ⚠️ **Важно:** Значение для CNAME может отличаться! Используйте то, что показал Vercel.

6. Нажмите **"Сохранить"**

### 3. Подождите распространения DNS

- Обычно занимает **5-30 минут**
- Может занять до **24 часов**

---

## 🔍 Проверка настроек

### Проверка DNS записей

Выполните в терминале:

```bash
# Проверка A-записи для корня
dig example.ru A

# Проверка CNAME для www
dig www.example.ru CNAME

# Проверка NS-записей
dig example.ru NS
```

### Проверка доступности

```bash
# Проверка HTTP ответа
curl -I https://example.ru
curl -I https://www.example.ru

# Проверка SSL сертификата
openssl s_client -connect example.ru:443 -servername example.ru
```

### Онлайн проверка

Используйте онлайн инструменты:
- [DNS Checker](https://dnschecker.org/)
- [What's My DNS](https://www.whatsmydns.net/)

---

## ⚠️ Частые проблемы

### 1. Домен не открывается

**Причины:**
- DNS записи еще не распространились (подождите до 24 часов)
- Неправильные DNS записи
- Домен не добавлен в Vercel

**Решение:**
1. Проверьте, что домен добавлен в Vercel
2. Проверьте DNS записи через `dig` или онлайн инструменты
3. Подождите распространения DNS (может занять до 24 часов)

### 2. SSL сертификат не работает

**Причина:** Vercel автоматически выдает SSL сертификат, но это может занять время.

**Решение:**
1. Убедитесь, что домен правильно настроен в Vercel
2. Подождите 5-10 минут после настройки DNS
3. Проверьте статус в Vercel Dashboard → Settings → Domains

### 3. Работает только www, но не корневой домен (или наоборот)

**Причина:** Настроена только одна запись (A или CNAME).

**Решение:**
- Настройте **обе** записи: A для корня и CNAME для www
- Или используйте редирект в Vercel (Settings → Domains → выберите домен → Configure)

### 4. "Invalid Configuration" в Vercel

**Причина:** DNS записи не совпадают с тем, что ожидает Vercel.

**Решение:**
1. Проверьте, что используете правильные значения из Vercel
2. Убедитесь, что DNS записи распространились (проверьте через `dig`)
3. Удалите домен из Vercel и добавьте заново

---

## 🚀 Быстрая диагностика

Выполните эти команды для проверки:

```bash
# 1. Проверка DNS
dig your-domain.ru A
dig www.your-domain.ru CNAME

# 2. Проверка доступности
curl -I https://your-domain.ru
curl -I https://www.your-domain.ru

# 3. Проверка SSL
openssl s_client -connect your-domain.ru:443 -servername your-domain.ru < /dev/null 2>/dev/null | openssl x509 -noout -dates
```

---

## 📞 Если ничего не помогло

1. **Проверьте логи в Vercel:**
   - Vercel Dashboard → Deployments → выберите последний деплой → View Function Logs

2. **Свяжитесь с поддержкой:**
   - [Поддержка Vercel](https://vercel.com/support)
   - [Поддержка Рег.ру](https://www.reg.ru/support/)

3. **Проверьте, нет ли блокировок:**
   - Проверьте файрвол/антивирус
   - Попробуйте с другого интернета/устройства
   - Проверьте, не заблокирован ли домен в файле hosts

---

## ⚙️ Шаг 3: Настройка переменной окружения NEXT_PUBLIC_SITE_URL

**ВАЖНО:** После настройки DNS добавьте переменную окружения в Vercel.

1. Откройте Vercel Dashboard → ваш проект → **Settings** → **Environment Variables**
2. Добавьте новую переменную:
   ```
   Name: NEXT_PUBLIC_SITE_URL
   Value: https://your-domain.ru (или https://www.your-domain.ru)
   Environment: ✅ Production ✅ Preview ✅ Development
   ```
3. Нажмите **"Save"**
4. Сделайте **Redeploy** последнего деплоя (Deployments → три точки → Redeploy)

Это нужно для правильной работы OAuth авторизации и email подтверждений.

---

## ✅ Чек-лист

- [ ] Домен добавлен в Vercel (Settings → Domains)
- [ ] DNS записи настроены в Рег.ру
- [ ] Подождали распространения DNS (минимум 5 минут)
- [ ] Проверили DNS через `dig` или онлайн инструменты
- [ ] Статус в Vercel показывает "Valid Configuration"
- [ ] Добавлена переменная `NEXT_PUBLIC_SITE_URL` в Vercel
- [ ] Сделан Redeploy после добавления переменной
- [ ] Сайт открывается по HTTPS
- [ ] SSL сертификат валидный

---

## 💡 Рекомендации

1. **Используйте DNS-серверы Vercel** (Способ 1) - это проще и надежнее
2. **Настройте редирект** в Vercel: корневой домен → www (или наоборот)
3. **Подождите распространения DNS** - не паникуйте, если не работает сразу
4. **Проверяйте через разные инструменты** - иногда локальный DNS кеш может показывать старые значения

