# 🔐 Настройка входа через Google

Это руководство поможет вам настроить OAuth авторизацию через Google в Supabase.

## 📋 Что нужно сделать

### 1. Создание OAuth клиента в Google Cloud Console

1. **Перейдите в Google Cloud Console:**
   - Откройте https://console.cloud.google.com/
   - Войдите в свой аккаунт Google

2. **Создайте новый проект или выберите существующий:**
   - Нажмите на выпадающий список проектов вверху
   - Нажмите "New Project"
   - Введите название проекта (например, "Proba Auth")
   - Нажмите "Create"

3. **Включите Google+ API:**
   - Перейдите в **APIs & Services** → **Library**
   - Найдите "Google+ API" или "People API"
   - Нажмите "Enable"

4. **Настройте OAuth consent screen:**
   - Перейдите в **APIs & Services** → **OAuth consent screen**
   - Выберите тип приложения (обычно "External")
   - Заполните обязательные поля:
     - **App name**: Название вашего приложения (например, "Proba")
     - **User support email**: Ваш email
     - **Developer contact information**: Ваш email
   - Нажмите "Save and Continue"
   - На странице "Scopes" нажмите "Save and Continue"
   - На странице "Test users" (если нужно) добавьте тестовых пользователей
   - Нажмите "Save and Continue"

5. **Создайте OAuth 2.0 Client ID:**
   - Перейдите в **APIs & Services** → **Credentials**
   - Нажмите "Create Credentials" → "OAuth client ID"
   - Выберите тип приложения: **Web application**
   - Заполните:
     - **Name**: Название (например, "Proba Web Client")
     - **Authorized JavaScript origins**: 
       ```
       http://localhost:3000
       https://yourdomain.com
       ```
       (замените `yourdomain.com` на ваш домен)
     - **Authorized redirect URIs**:
       ```
       https://[YOUR-PROJECT-REF].supabase.co/auth/v1/callback
       ```
       (замените `[YOUR-PROJECT-REF]` на ваш Project Reference из Supabase)
   
   **Где найти Project Reference в Supabase:**
   - Откройте ваш проект в Supabase Dashboard
   - Перейдите в **Settings** → **API**
   - Project Reference находится в разделе "Project URL"
   - Например, если URL: `https://abcdefghijk.supabase.co`, то Reference: `abcdefghijk`

6. **Скопируйте Client ID и Client Secret:**
   - После создания вы увидите модальное окно с данными
   - **Сохраните Client ID и Client Secret** (они понадобятся в следующем шаге)

### 2. Настройка Google провайдера в Supabase

1. **Откройте Supabase Dashboard:**
   - Перейдите на https://supabase.com/dashboard
   - Выберите ваш проект

2. **Перейдите в настройки аутентификации:**
   - В левом меню: **Authentication** → **Providers**
   - Найдите провайдер **Google**

3. **Включите Google провайдер:**
   - Переключите переключатель **Enable Google provider** в положение "ON"

4. **Вставьте данные из Google Cloud Console:**
   - **Client ID (for OAuth)**: Вставьте Client ID из Google Cloud Console
   - **Client Secret (for OAuth)**: Вставьте Client Secret из Google Cloud Console

5. **Нажмите "Save"**

### 3. Настройка Redirect URL в Supabase

1. **Проверьте Redirect URLs:**
   - В Supabase Dashboard: **Authentication** → **URL Configuration**
   - Убедитесь, что **Site URL** установлен правильно:
     ```
     http://localhost:3000 (для разработки)
     https://yourdomain.com (для продакшена)
     ```
   - Проверьте, что в **Redirect URLs** добавлены:
     ```
     http://localhost:3000/auth/callback
     https://yourdomain.com/auth/callback
     ```

2. **Если нужно добавить новый Redirect URL:**
   - Нажмите "Add URL"
   - Введите URL (например, `http://localhost:3000/auth/callback`)
   - Нажмите "Save"

### 4. Переменные окружения (опционально)

Если вы хотите указать фиксированный Site URL в переменных окружения:

```env
NEXT_PUBLIC_SITE_URL=https://yourdomain.com
```

Добавьте это в `.env.local` для локальной разработки и в настройках Vercel для продакшена.

## ✅ Проверка работы

1. **Локально:**
   ```bash
   npm run dev
   ```
   - Откройте http://localhost:3000/login
   - Нажмите "Войти через Google"
   - Должно произойти перенаправление на Google для авторизации

2. **После авторизации:**
   - После успешной авторизации вы будете перенаправлены обратно на `/auth/callback`
   - Затем автоматически перенаправление на `/specialists`

## 🔧 Возможные проблемы

### Ошибка "redirect_uri_mismatch"
- **Причина**: Redirect URI в Google Cloud Console не совпадает с тем, что использует Supabase
- **Решение**: 
  1. Проверьте, что в Google Cloud Console добавлен правильный Redirect URI:
     ```
     https://[YOUR-PROJECT-REF].supabase.co/auth/v1/callback
     ```
  2. Убедитесь, что вы используете правильный Project Reference

### Кнопка Google не отображается
- **Причина**: Supabase не настроен или отсутствуют переменные окружения
- **Решение**: Проверьте, что в `.env.local` указаны:
  ```
  NEXT_PUBLIC_SUPABASE_URL=...
  NEXT_PUBLIC_SUPABASE_ANON_KEY=...
  ```

### Ошибка "invalid_client"
- **Причина**: Неверный Client ID или Client Secret
- **Решение**: Проверьте, что данные в Supabase Dashboard совпадают с данными из Google Cloud Console

### Пользователь не создается в базе данных
- **Причина**: Профиль специалиста не создается автоматически
- **Решение**: Проверьте консоль браузера и логи. Функция `ensureSpecialistProfile` должна автоматически создавать профиль при первом входе через OAuth

## 📚 Дополнительная информация

- [Документация Supabase OAuth](https://supabase.com/docs/guides/auth/social-login/auth-google)
- [Документация Google OAuth 2.0](https://developers.google.com/identity/protocols/oauth2)

## 🎉 Готово!

После выполнения всех шагов вход через Google должен работать. Пользователи смогут регистрироваться и входить, используя свои Google аккаунты.

