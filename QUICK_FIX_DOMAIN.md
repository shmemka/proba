# 🚨 Быстрое решение: Домен не работает

## Проблема
Сайт работает на `your-project.vercel.app`, но не работает на купленном домене от Рег.ру.

## ⚡ Быстрое решение (5 минут)

### 1. Добавьте домен в Vercel

1. Откройте [Vercel Dashboard](https://vercel.com) → ваш проект
2. **Settings** → **Domains** → **Add Domain**
3. Введите ваш домен: `your-domain.ru`
4. Введите также: `www.your-domain.ru`
5. Нажмите **Add**

### 2. Настройте DNS в Рег.ру (выберите один способ)

#### 🔹 Способ A: DNS-серверы Vercel (РЕКОМЕНДУЕТСЯ)

1. В Vercel скопируйте DNS-серверы (обычно `ns1.vercel-dns.com` и `ns2.vercel-dns.com`)
2. В Рег.ру: **Домены** → ваш домен → **DNS-серверы**
3. Выберите **"Использовать DNS-серверы"**
4. Вставьте DNS-серверы Vercel
5. **Сохраните**

#### 🔹 Способ B: DNS-записи в Рег.ру

1. В Vercel скопируйте значения для DNS-записей
2. В Рег.ру: **Домены** → ваш домен → **DNS-записи**
3. Добавьте записи, которые показал Vercel:
   - A-запись для корня (`@`)
   - CNAME для `www`
4. **Сохраните**

### 3. Добавьте переменную окружения

1. Vercel Dashboard → **Settings** → **Environment Variables**
2. Добавьте:
   ```
   Name: NEXT_PUBLIC_SITE_URL
   Value: https://your-domain.ru
   Environment: Production, Preview, Development
   ```
3. **Save**
4. **Deployments** → последний деплой → **Redeploy**

### 4. Подождите 5-30 минут

DNS записи распространяются не мгновенно. Подождите и проверьте статус в Vercel.

---

## 🔍 Проверка

### Проверьте DNS:
```bash
dig your-domain.ru A
dig www.your-domain.ru CNAME
```

### Проверьте доступность:
```bash
curl -I https://your-domain.ru
```

### Проверьте в Vercel:
- Settings → Domains → статус должен быть **"Valid Configuration"** ✅

---

## ❌ Если не работает

1. **Проверьте DNS кеш:**
   ```bash
   # macOS
   sudo dscacheutil -flushcache
   sudo killall -HUP mDNSResponder
   
   # Windows
   ipconfig /flushdns
   ```

2. **Проверьте HSTS кеш в браузере:**
   - Chrome: `chrome://net-internals/#hsts`
   - Удалите ваш домен из списка

3. **Попробуйте в приватном окне**

4. **Проверьте, что домен добавлен в Vercel** и статус "Valid Configuration"

5. **Подождите до 24 часов** - иногда DNS распространяется медленно

---

## 📞 Нужна помощь?

Смотрите подробную инструкцию: `DOMAIN_SETUP_REG.RU.md`

