# 🔧 Исправление ошибки portfolio_preview в Supabase

## ❌ Проблема

Ошибка: `Could not find the 'portfolio_preview' column of 'specialists' in the schema cache`

Эта ошибка возникает, потому что в вашей базе данных Supabase еще не создана колонка `portfolio_preview` в таблице `specialists`.

---

## ✅ Решение

### Вариант 1: Быстрое исправление (рекомендуется)

1. Откройте ваш проект в Supabase: https://supabase.com/dashboard
2. Перейдите в **SQL Editor** (🗄️ в левом меню)
3. Нажмите **"New query"**
4. Откройте файл **`supabase-add-portfolio-preview.sql`** в вашем проекте
5. Скопируйте **весь код** из файла
6. Вставьте в SQL Editor
7. Нажмите **"Run"** (или Ctrl/Cmd + Enter)
8. Дождитесь сообщения: ✅ **"Success. No rows returned"** или **"Добавлена колонка portfolio_preview"**

Готово! Колонка добавлена, ошибка исчезнет.

---

### Вариант 2: Полная миграция (если нужно обновить всю структуру)

Если ваша база данных создана по старой схеме, выполните полную миграцию:

1. В Supabase откройте **SQL Editor**
2. Выполните файл **`supabase-migration.sql`** (он создаст все таблицы с нуля)

⚠️ **ВНИМАНИЕ**: Это удалит все существующие данные! Используйте только для новых проектов или если данные можно потерять.

---

### Вариант 3: Безопасная миграция (если нужно сохранить данные)

Если у вас уже есть данные и таблица `projects` использует `company_id` вместо `user_id`:

1. В Supabase откройте **SQL Editor**
2. Выполните файл **`supabase-migrate-to-user-id.sql`** (безопасно перенесет данные)

---

## 🧪 Проверка

После выполнения миграции:

1. Откройте **Table Editor** в Supabase
2. Выберите таблицу **`specialists`**
3. Проверьте, что в колонках есть **`portfolio_preview`** (тип: `text[]`)

Если колонка есть — всё готово! ✅

---

## 📋 Пошаговая инструкция (для начинающих)

### Шаг 1: Откройте Supabase

1. Перейдите на https://supabase.com
2. Войдите в свой аккаунт
3. Выберите ваш проект

### Шаг 2: Откройте SQL Editor

1. В левом меню найдите иконку 🗄️ **SQL Editor**
2. Нажмите на неё
3. Нажмите кнопку **"New query"** вверху

### Шаг 3: Скопируйте SQL код

1. Откройте файл `supabase-add-portfolio-preview.sql` в вашем проекте (в редакторе кода)
2. Выделите весь код (Ctrl+A / Cmd+A)
3. Скопируйте (Ctrl+C / Cmd+C)

### Шаг 4: Выполните SQL

1. Вставьте код в SQL Editor Supabase (Ctrl+V / Cmd+V)
2. Нажмите кнопку **"Run"** (или клавишу Ctrl/Cmd + Enter)
3. Дождитесь результата внизу

Должно появиться сообщение:
- ✅ **"Success. No rows returned"** или
- ✅ **"Добавлена колонка portfolio_preview"**

### Шаг 5: Проверьте результат

1. В левом меню откройте **Table Editor**
2. Выберите таблицу **`specialists`**
3. Посмотрите на колонки — должна быть колонка **`portfolio_preview`**

### Шаг 6: Обновите приложение

1. Перезагрузите страницу в браузере (F5)
2. Ошибка должна исчезнуть

---

## 🔍 Дополнительная проверка

Выполните этот SQL запрос, чтобы проверить структуру таблицы:

```sql
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'specialists' 
AND column_name = 'portfolio_preview';
```

Если видите строку с `portfolio_preview` и типом `ARRAY` — всё работает! ✅

---

## ❓ Что делать, если ничего не помогло?

1. **Проверьте, что вы работаете с правильным проектом** в Supabase
2. **Убедитесь, что таблица `specialists` существует** (Table Editor)
3. **Попробуйте выполнить SQL запрос вручную:**
   ```sql
   ALTER TABLE specialists ADD COLUMN IF NOT EXISTS portfolio_preview TEXT[] DEFAULT '{}'::text[];
   ```
4. **Проверьте права доступа** — возможно, нужны права администратора

---

## 🎉 После исправления

После добавления колонки `portfolio_preview`:

- ✅ Ошибка исчезнет
- ✅ Загрузка специалистов будет работать быстрее
- ✅ Превью портфолио будет отображаться в каталоге

---

## 📦 Если видите ошибку "Bucket not found"

Это значит, что нужно настроить Supabase Storage для загрузки изображений:

1. Откройте **Storage** → **Buckets** в Supabase
2. Создайте bucket с именем `public-assets` (Public bucket ✅)
3. Настройте политики доступа (см. **SUPABASE_STORAGE_SETUP.md**)

Подробная инструкция: [SUPABASE_STORAGE_SETUP.md](./SUPABASE_STORAGE_SETUP.md)

---

## 📞 Нужна помощь?

Если проблема осталась:
1. Скопируйте полный текст ошибки из консоли браузера (F12)
2. Проверьте логи в Supabase: **Logs** → **Postgres Logs**
3. Убедитесь, что выполняете SQL в правильном проекте

---

**Готово!** Выполните `supabase-add-portfolio-preview.sql` и всё заработает! 🚀

